name: import_guard_example
description: Example package to test import_guard
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  # Fake packages for testing
  # In real usage, these would be actual packages

dev_dependencies:
  import_guard:
    path: ..
  custom_lint: ^0.8.0
